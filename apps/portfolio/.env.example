# Privacy-conscious analytics (disabled by default)
# Set to "true" to send events to the configured endpoint.
VITE_ANALYTICS_ENABLED=false

# Optional collector URL that accepts POST JSON payloads:
# { event, properties, ts, path }
# VITE_ANALYTICS_ENDPOINT=https://example.com/collect
